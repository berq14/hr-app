# Örnek ortam değişkenleri — kopyalayıp .env olarak kaydedin ve değerleri değiştirin.
# Anahtar üretmek için: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

DATABASE_URL="file:./dev.db"
FIELD_ENCRYPTION_KEY="<32-bayt-hex>"
SESSION_SECRET="<32-bayt-hex>"
INDEX_HMAC_KEY="<32-bayt-hex>"
APP_URL="http://localhost:3000"
